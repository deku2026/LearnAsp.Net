# Campus W3 (Step09–10) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or executing-plans. Checkboxes track progress.

**Goal:** Complete Capstone 1 with Step09 (WAF + real PostgreSQL integration tests) and Step10 (Kestrel + typed HttpClient resilience).

**Architecture:** Independent labs; Step09 uses EF Core + Npgsql against Testcontainers (fallback: local Docker PG on localhost:5432). Step10 uses `IHttpClientFactory` + `AddStandardResilienceHandler` and WireMock in tests.

**Tech Stack:** net10.0, Testcontainers.PostgreSql 4.13, Respawn 7, EF Core Npgsql 10.0.3, Microsoft.Extensions.Http.Resilience 10.8, WireMock.Net 2.12

## Global Constraints

- Campus domain; no MediatR/AutoMapper/MassTransit/FluentAssertions
- Ports 5009 / 5010
- Spec: `docs/superpowers/specs/2026-07-18-campus-learnaspnet-design.md` §4 W3
- Docker optional at runtime for app; required or localhost PG for Step09 tests
- Branch: `feature/campus-w3-steps09-10`

---

### Task 1: Packages + slnx test projects

- [ ] CPM: Testcontainers.PostgreSql, Respawn, Npgsql.EFCore, Http.Resilience, WireMock.Net
- [ ] Add test projects to slnx
- [ ] Commit

### Task 2: Step09 app (EF Campus API + auth)

- [ ] CampusDbContext + entities + EnsureCreated
- [ ] Courses/Sections/Enrollments API + validation + DevTest auth
- [ ] ConnectionStrings:Postgres from config

### Task 3: Step09 tests (Testcontainers + Respawn + WAF)

- [ ] Fixture: start PG container or use localhost
- [ ] Inject connection string via WebApplicationFactory
- [ ] Tests: CRUD, 400 validation, 401/403 auth, Respawn between tests
- [ ] Skip cleanly if neither Docker nor local PG

### Task 4: Step10 app (Kestrel + typed client)

- [ ] Kestrel limits configuration
- [ ] `ExternalCatalogClient` + standard resilience
- [ ] `/proxy/catalog/{code}`, `/kestrel-limits`

### Task 5: Step10 tests (WireMock)

- [ ] WireMock stub external catalog
- [ ] Proxy success + resilience does not break happy path
- [ ] Kestrel endpoint returns configured limits

### Task 6: Verify + push

- [ ] `dotnet test` all green
- [ ] Push branch; open PR optional

---

## Verification

```pwsh
cd .worktrees/campus-w3-steps09-10
dotnet test LearnAspNet.slnx -c Release
```
